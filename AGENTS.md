## Learned User Preferences

- **NO HALF-ASSING. BASIC IS NOT AN OPTION.** Every pass must be production-grade end-to-end — audit after implementing, verify the actual user-visible behavior (app starts, resize works, icons fill the taskbar, uninstalled plugins stay gone), fix root causes, and keep hunting unfinished wiring / other crashes instead of stopping at the first bug. Shipping a partial/cosmetic change that still fails the reported bug is unacceptable.
- Do not use bandaid fixes; implement core-issue fixes and improvements—do not strip libraries (e.g. Three.js for 3D preview) or downgrade capabilities to paper over bugs.
- Expect top-tier UI and backend; nothing basic, half-assed, or placeholder when full features were requested; generic React/SaaS-looking cards are not allowed for workspace tools (Automation / Spatial); Automation inspector/sidebar on single-click select with card details toggling on mousedown, not double-click.
- Only commit git changes when explicitly asked; do not run `git revert` without permission—restore uncommitted work via manual file edits.
- Spacedrive/Spacebot work must extract actual repo files and wire user-visible features into BNDZ FM surfaces—not sidecar plumbing, iframe embeds, vendor dumps, or engine admin UI; packaging/build milestones are not integration done.
- Native-integrate host features end-to-end (especially RAM Staging via ImDisk / Arsenal Image Mounter)—no install/driver download prompts, sidebar install UX, leftover “AIM drivers / ImDisk ready” chrome, or requiring users to fetch drivers/API keys separately.
- Follow clear user direction and attached plans without reframing, stopping early, or substituting a narrower scope; keep implementing until work is actually correct, then continue upgrading; verify/debug/code-review before claiming done; user tests only after all planned phases are completely done.
- Selling-point / “net-new” work must be unique category-defining features people cannot get in other file managers—not competitor parity, not music-niche-only, and not upgrades of already-shipped plugins dressed up as new (fold related capability into the existing plugin instead); each selling to-do needs 3 distinct implementation paths (parity upgrades go in `to-do-future-upgrades.md`). When the core FM is fragile, prioritize balanced core audits/fixes over more plugin chrome.
- Floating action bar (Command Deck above the bottom plugin panel): distinctive themed design with user-editable color/gradient in Colors settings; only show actions for installed plugins—never auto-install on click; new marketplace plugins ship Uninstalled by default; re-sync install state when config changes (never freeze first hydrate); in FilesMerge, Command Deck defaults **off** and the omnibar Plugins/Smart Tools button strip must stay gone; Midnight default theme; branch bar and mini tree disabled by default (mini tree in its proper slot); default installed plugins only System Properties, Fast Search, Visual Filters; slightly narrower default preview width.
- Preserve listview interaction (drag, virtualized marquee, Ctrl+marquee, empty-space deselect) so actions do not block each other; multi-item drag must use one animated fan/span stack without a second single-item ghost covering it; keep desktop→BNDZ external drop working without regressing it; optimistic move/delete/rename must update the list immediately without flicker-back-then-refresh; list open/scroll/drag/marquee must feel Explorer-native (instant icons, smooth FPS, GPU-accelerated)—not pop-in icons or ~10fps interaction; default list selection highlight magenta/purple; sidebar tree nodes support drag/reorder with Files-modern row sizing.
- FilesMerge direction (user-confirmed): keep Files WinUI **list/tree** (and related browsing engines) as the file browser; blend **full BNDZ frontend craft** (theme, tabs/toolbar/menubar skin, plugins dock, preview, Automation/Spatial/Config panes)—not a React list/tree takeover, not HWND embed, not fragmented `?pane=` shells replacing ContentRow. When Files vs BNDZ behavior diverges, expose both as optional settings. BNDZ React panes must open instantly—no repeated “Loading BNDZ” splash per pane; panels must honor theme/colors. Standalone classic BNDZUI outer columns: sidebar/tree **12%** / workspace **72%** / preview **16%** (preview wider than tree); do not shrink the bottom plugin panel; do not remove core sidebar modules (e.g. Drives); Smart Views must not auto-pick view modes; sidebar tree neon modules stay vivid (not dimmed).
- Always apply `.cursor/rules/bndz-implementation-rule.mdc` and `.cursor/rules/bndz-uiverse-ui.mdc` (fresh `npm` + Debug `dotnet` builds; Uiverse craft for all UI including Automation/Spatial cards). Taskbar/exe ApplicationIcon must be a **multi-resolution** ICO (16–256) generated with fill/zoom so the glyph is large and crisp — a single 256-only or tiny-downscaled ICO is not acceptable; do not change the in-UI WebView favicon unless asked.

## Learned Workspace Facts

- BNDZ is a next-gen Windows file manager competing with File Pilot and XYplorer; product path includes classic C# `BNDZBackend` + **WebView2/React** `src/` (stay on WebView2—no alternate FM UI shell), plus **FilesMerge** hybrid (`FilesMerge/` WinUI shell hosting BNDZ panes).
- **BNDZ-Native (primary):** `BNDZShell/` WinUI greenfield — one full BNDZUI face via `CraftPaneHost` (`?nativeShell=1`); React list owns FS listing through in-process `BndzIpcHost` / `BndzEmbeddedBackendHost` (no WPF App/MainWindow UI, no `--backend-host` sidecar). `NativeListHost` kept collapsed until a non-overlapping grid layout can host it without multi-WebView fragmentation. Unpackaged + `WindowsAppSDKSelfContained` — double-click `BNDZShell.exe` or `scripts/run-bndz-native.cmd`. Caption X → Ask/Tray/Quit; Win32 tray; Always on Top restores. Build: `scripts/build-bndz-native.ps1`.
- FilesMerge (archived reference): Files WinUI + `BndzPaneHost` + sidecar `BNDZ.exe --backend-host` — do not extend as product path.
- Spacedrive/Spacebot upstream repos at `external/spacedrive` and `external/spacebot` are **UX/reference only** — copy UI patterns into BNDZ components; wire to native C# services (`BndzFileIndexService`, `AiAssistantService`, `DuplicateFinderService`), never `src/engines/` HTTP bridges or sidecar admin UI.
- File index / icon / size cache: `BNDZBackend/Services/BndzFileIndexService.cs` (SQLite under `%LocalAppData%/BNDZ/Index/`) plus native icon/thumbnail caching—icons and sizes must be warm on folder open like Explorer (no wait-for-pop-in on revisit) and must not re-fetch on every navigate.
- Classic list interaction logic primarily in `src/components/BNDZUI.tsx` and `src/lib/dragController.ts`; multi-item fan/span drag UI lives under `src/workstation/drag/` (e.g. `FluidDragStack`); details view must not shift layout vs other views; list rendering should lean on GPU/compositor-friendly paths so drag, marquee, and scroll stay smooth.
- META upgrades: deepen NuGet-backed host services into list/preview/search/metadata surfaces via `src/lib/ipcBridge.ts`; see `.cursor/rules/bndz-implementation-rule.mdc`. Selling-pillar host services include ProjectSandbox, LibraryHealth, CapacitySolver, InboundVolume, FileLineage, BudgetGovernor, and BranchingTime—wired into plugins/list/preview, not stub UIs; related capability belongs in existing plugins rather than parallel “new” plugin skins.
- Default outer workspace layout in `src/lib/workspaceLayout.ts`: sidebar **12%**, workspace **72%**, preview **16%** (v61; preview wider than tree) for classic BNDZUI; breadcrumb bar (`src/components/BreadcrumbTrail.tsx`) keeps path visible via flex/min-width—avoid premature ellipsis when the views bar has room; sidebar tree must keep a working Drives module and must not park BNDZ Portal / Twin Volume Chess as tree roots.
- RAM Staging is a native host feature (ImDisk / Arsenal Image Mounter wired into BNDZBackend)—not a separate installer wizard or left-sidebar install prompt; staged-area ops (e.g. rename) must work as a real drive.
- Workspace tools Automation and Spatial Canvas are primary selling surfaces and must be feature-complete, distinctive, and performant—not thin single-purpose canvases or laggy card drag.
- Preview stack keeps Three.js for 3D model viewing; Loupe/Luma must not shrink viewed content to a tiny irrecoverable scale or throw the image on pan; audio/music tooling (BPM/key detection, waveform editor) is an intentional producer focus—not the sole selling-pillar theme.
- Agent work queue: mandatory stabilization in `to-do.md` (Phases 1-8); **category-defining** selling pillars in `to-do-selling-points.md` (compose RAM+Ghost+Spatial+Rack+Producer — not competitor parity; each pillar with 3 implementation paths); parity backlog in `to-do-future-upgrades.md`; FilesMerge native-FM track in `to-do-native-fm.md` / `BNDZ_NATIVE.md`.
- Core FM reliability (startup integrity, list perf, external drop, Command Deck install gating, taskbar icon, FilesMerge host process hygiene) outranks additive plugin surface area when the product is fragile.

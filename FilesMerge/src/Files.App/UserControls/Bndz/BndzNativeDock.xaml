<!-- Copyright (c) BNDZ. WinUI Command Deck + plugin chrome (Files grid rhythm). -->
<UserControl
	x:Class="Files.App.UserControls.Bndz.BndzNativeDock"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid Background="{ThemeResource App.Theme.FileArea.SecondaryBackgroundBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="Auto" />
			<RowDefinition Height="Auto" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>

		<!-- Command Deck strip -->
		<Border
			x:Name="DeckStrip"
			Padding="10,8"
			Background="{StaticResource BndzDeckFillBrush}"
			BorderBrush="{StaticResource BndzDividerBrush}"
			BorderThickness="0,1,0,0">
			<Grid>
				<Grid.ColumnDefinitions>
					<ColumnDefinition Width="Auto" />
					<ColumnDefinition Width="*" />
				</Grid.ColumnDefinitions>

				<Border
					x:Name="SelectionBadge"
					Padding="12,6"
					Margin="0,0,10,0"
					VerticalAlignment="Center"
					Background="{StaticResource BndzSurfaceRaisedBrush}"
					BorderBrush="{StaticResource BndzDeckBorderBrush}"
					BorderThickness="1"
					CornerRadius="{StaticResource BndzChipRadius}">
					<TextBlock
						x:Name="SelectionBadgeText"
						FontSize="12"
						FontWeight="SemiBold"
						Foreground="{StaticResource BndzInkBrush}"
						Text="No selection" />
				</Border>

				<ScrollViewer
					Grid.Column="1"
					HorizontalScrollBarVisibility="Auto"
					HorizontalScrollMode="Enabled"
					VerticalScrollBarVisibility="Disabled"
					VerticalScrollMode="Disabled">
					<ItemsControl x:Name="DeckToolsHost">
						<ItemsControl.ItemsPanel>
							<ItemsPanelTemplate>
								<StackPanel Orientation="Horizontal" Spacing="6" />
							</ItemsPanelTemplate>
						</ItemsControl.ItemsPanel>
					</ItemsControl>
				</ScrollViewer>
			</Grid>
		</Border>

		<!-- Plugin tab strip -->
		<Border
			Grid.Row="1"
			Padding="6,0,6,0"
			Background="{StaticResource BndzSurfaceBrush}"
			BorderBrush="{StaticResource BndzDividerBrush}"
			BorderThickness="0,0,0,1">
			<ScrollViewer
				HorizontalScrollBarVisibility="Auto"
				HorizontalScrollMode="Enabled"
				VerticalScrollBarVisibility="Disabled"
				VerticalScrollMode="Disabled">
				<ItemsControl x:Name="PluginTabsHost">
					<ItemsControl.ItemsPanel>
						<ItemsPanelTemplate>
							<StackPanel Orientation="Horizontal" Spacing="2" />
						</ItemsPanelTemplate>
					</ItemsControl.ItemsPanel>
				</ItemsControl>
			</ScrollViewer>
		</Border>

		<!-- Plugin body -->
		<ContentControl
			x:Name="PluginBodyHost"
			Grid.Row="2"
			HorizontalContentAlignment="Stretch"
			VerticalContentAlignment="Stretch"
			Background="{StaticResource BndzSurfaceBrush}" />
	</Grid>
</UserControl>

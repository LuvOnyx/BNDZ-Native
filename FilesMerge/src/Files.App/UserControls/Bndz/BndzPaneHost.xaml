<!--  Copyright (c) BNDZ — FilesMerge hosted React pane (architecture #3).  -->
<UserControl
	x:Class="Files.App.UserControls.Bndz.BndzPaneHost"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	d:DesignHeight="280"
	d:DesignWidth="800"
	mc:Ignorable="d">

	<!-- Flush host: no floating card chrome — Files geometry owns the frame. -->
	<Grid Background="#0C0F14">
		<WebView2
			x:Name="PaneWebView"
			HorizontalAlignment="Stretch"
			VerticalAlignment="Stretch"
			DefaultBackgroundColor="#0C0F14"
			Loaded="PaneWebView_Loaded" />
		<TextBlock
			x:Name="PaneStatusHint"
			Margin="16"
			HorizontalAlignment="Center"
			VerticalAlignment="Center"
			Foreground="{ThemeResource TextFillColorSecondaryBrush}"
			IsHitTestVisible="False"
			Text="Loading BNDZ pane…"
			Visibility="Collapsed" />
	</Grid>
</UserControl>

<UserControl
    x:Class="BNDZShell.Bndz.NativeListHost"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="#FF0C0F14"
    HorizontalAlignment="Stretch"
    VerticalAlignment="Stretch"
    IsHitTestVisible="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Column header strip — soft glass bar -->
        <Border
            Grid.Row="0"
            Padding="14,8,14,8"
            BorderBrush="#22FFFFFF"
            BorderThickness="0,0,0,1">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#33282C34" Offset="0" />
                    <GradientStop Color="#2216181E" Offset="1" />
                </LinearGradientBrush>
            </Border.Background>
            <Grid ColumnDefinitions="*,120,110">
                <TextBlock Text="Name" Foreground="#B8C0CC" FontSize="12" FontWeight="SemiBold" />
                <TextBlock Grid.Column="1" Text="Type" Foreground="#8B93A1" FontSize="12" Margin="12,0,0,0" />
                <TextBlock Grid.Column="2" Text="Size" Foreground="#8B93A1" FontSize="12" HorizontalAlignment="Right" />
            </Grid>
        </Border>

        <ListView
            x:Name="FileList"
            Grid.Row="1"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            Background="Transparent"
            IsItemClickEnabled="True"
            SelectionMode="Extended"
            CanDragItems="True"
            DragItemsStarting="FileList_DragItemsStarting"
            ItemClick="FileList_ItemClick"
            SelectionChanged="FileList_SelectionChanged">
            <ListView.Resources>
                <SolidColorBrush x:Key="ListViewItemBackground" Color="Transparent" />
                <SolidColorBrush x:Key="ListViewItemBackgroundPointerOver" Color="#33C026D3" />
                <SolidColorBrush x:Key="ListViewItemBackgroundSelected" Color="#66C026D3" />
                <SolidColorBrush x:Key="ListViewItemBackgroundSelectedPointerOver" Color="#88C026D3" />
                <SolidColorBrush x:Key="ListViewItemPointerOverBackgroundThemeBrush" Color="#33C026D3" />
                <SolidColorBrush x:Key="ListViewItemSelectedBackgroundThemeBrush" Color="#66C026D3" />
                <SolidColorBrush x:Key="ListViewItemSelectedPointerOverBackgroundThemeBrush" Color="#88C026D3" />
            </ListView.Resources>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="6,2" />
                    <Setter Property="MinHeight" Value="32" />
                    <Setter Property="Margin" Value="4,1" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ColumnDefinitions="*,120,110" Padding="8,6" CornerRadius="8">
                        <TextBlock
                            Text="{Binding Name}"
                            Foreground="#E8EEF6"
                            FontSize="13"
                            FontWeight="Medium"
                            TextTrimming="CharacterEllipsis"
                            VerticalAlignment="Center" />
                        <TextBlock
                            Grid.Column="1"
                            Margin="12,0,0,0"
                            Foreground="#9CA3AF"
                            FontSize="12"
                            Text="{Binding TypeLabel}"
                            TextTrimming="CharacterEllipsis"
                            VerticalAlignment="Center" />
                        <TextBlock
                            Grid.Column="2"
                            Foreground="#A5B0C0"
                            FontSize="12"
                            Text="{Binding SizeLabel}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <Border
            Grid.Row="2"
            Padding="14,6"
            BorderBrush="#18FFFFFF"
            BorderThickness="0,1,0,0"
            Background="#18000000">
            <TextBlock
                Text="{Binding StatusText, Mode=OneWay}"
                Foreground="#8B93A1"
                FontSize="11" />
        </Border>
    </Grid>
</UserControl>

<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

  <!-- BNDZ native shell tokens — soft squircles, paper depth, not generic SaaS chrome. -->
  <Color x:Key="BndzBgDeep">#FF12151A</Color>
  <Color x:Key="BndzBgPanel">#FF1A1F27</Color>
  <Color x:Key="BndzBgRaised">#FF232A35</Color>
  <Color x:Key="BndzStroke">#33FFFFFF</Color>
  <Color x:Key="BndzText">#FFF2F4F7</Color>
  <Color x:Key="BndzTextMuted">#FF9AA3B2</Color>
  <Color x:Key="BndzAccent">#FF5B8CFF</Color>
  <Color x:Key="BndzAccentSoft">#335B8CFF</Color>

  <SolidColorBrush x:Key="BndzBgDeepBrush" Color="{StaticResource BndzBgDeep}" />
  <SolidColorBrush x:Key="BndzBgPanelBrush" Color="{StaticResource BndzBgPanel}" />
  <SolidColorBrush x:Key="BndzBgRaisedBrush" Color="{StaticResource BndzBgRaised}" />
  <SolidColorBrush x:Key="BndzStrokeBrush" Color="{StaticResource BndzStroke}" />
  <SolidColorBrush x:Key="BndzTextBrush" Color="{StaticResource BndzText}" />
  <SolidColorBrush x:Key="BndzTextMutedBrush" Color="{StaticResource BndzTextMuted}" />
  <SolidColorBrush x:Key="BndzAccentBrush" Color="{StaticResource BndzAccent}" />
  <SolidColorBrush x:Key="BndzAccentSoftBrush" Color="{StaticResource BndzAccentSoft}" />

  <CornerRadius x:Key="BndzSquircle">14</CornerRadius>
  <CornerRadius x:Key="BndzSquircleSm">10</CornerRadius>

  <Style x:Key="BndzPanelBorder" TargetType="Border">
    <Setter Property="Background" Value="{StaticResource BndzBgPanelBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource BndzStrokeBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="CornerRadius" Value="{StaticResource BndzSquircle}" />
    <Setter Property="Padding" Value="10" />
  </Style>

  <Style x:Key="BndzGhostButton" TargetType="Button">
    <Setter Property="Background" Value="Transparent" />
    <Setter Property="Foreground" Value="{StaticResource BndzTextBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource BndzStrokeBrush}" />
    <Setter Property="BorderThickness" Value="1" />
    <Setter Property="Padding" Value="10,6" />
    <Setter Property="Cursor" Value="Hand" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="Button">
          <Border x:Name="Bd"
                  Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="{StaticResource BndzSquircleSm}"
                  Padding="{TemplateBinding Padding}">
            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center" />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource BndzAccentSoftBrush}" />
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
              <Setter Property="Opacity" Value="0.45" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

  <Style x:Key="BndzPrimaryButton" TargetType="Button" BasedOn="{StaticResource BndzGhostButton}">
    <Setter Property="Background" Value="{StaticResource BndzAccentSoftBrush}" />
    <Setter Property="BorderBrush" Value="{StaticResource BndzAccentBrush}" />
  </Style>

  <Style x:Key="BndzListRow" TargetType="ListViewItem">
    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
    <Setter Property="Padding" Value="0" />
    <Setter Property="Margin" Value="0,1" />
    <Setter Property="Template">
      <Setter.Value>
        <ControlTemplate TargetType="ListViewItem">
          <Border x:Name="Bd"
                  Background="Transparent"
                  CornerRadius="{StaticResource BndzSquircleSm}"
                  Padding="10,8"
                  SnapsToDevicePixels="True">
            <ContentPresenter />
          </Border>
          <ControlTemplate.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="#22FFFFFF" />
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
              <Setter TargetName="Bd" Property="Background" Value="{StaticResource BndzAccentSoftBrush}" />
            </Trigger>
          </ControlTemplate.Triggers>
        </ControlTemplate>
      </Setter.Value>
    </Setter>
  </Style>

</ResourceDictionary>
